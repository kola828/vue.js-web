export default {

  // doneTodos: state => {
  //   return state.todos.filter(todo => todo.done)
  // },

  getInfo(state){
    // console.log(state);
    // let param={
    //   headImg:state.userInfo.avatar_url,
    //   userName:state.userInfo.loginname,
    //   score:state.userInfo.score,
    //   recentRep:state.userInfo.recent_replies
    // };
    // return param
  }

}